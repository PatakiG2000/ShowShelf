import * as React from "react";
import Ratingform from "../../Layout/Ratingform";

export interface IRateButtonProps {}

export default function RateButton(props: IRateButtonProps) {
  return (
    <>
      <button onClick={"asd"} className="btn">
        Rate
      </button>
    </>
  );
}
